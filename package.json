{
  "name": "thinknine-bc-process-intelligence",
  "version": "3.7.0",
  "private": true,
  "description": "Modular Edge-only Business Central process intelligence and documentation engine.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node tests/engine.test.js && node tests/review.test.js && node tests/word-exporter.test.js",
    "check": "node --check dist/background.js && node --check dist/content.js && node --check dist/dashboard.js",
    "lint": "node scripts/lint.js",
    "release:zip": "node scripts/release.js",
    "ci": "npm run lint && npm test && npm run build && npm run check",
    "dev:build": "node scripts/build.js",
    "dev:check": "npm run build && npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
